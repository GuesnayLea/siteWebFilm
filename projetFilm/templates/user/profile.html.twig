{% extends 'base.html.twig' %}

{% block title %}Mon Profil - {{ user.nom }}{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <!--infos perso sur l'utilisateur-->
        <div class="col-md-4">
            <div class="card mb-4">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="bi bi-person-circle"></i> Informations personnelles</h5>
                </div>
                <div class="card-body">
                    <div class="text-center mb-4">
                        <i class="bi bi-person-circle display-1 text-primary"></i>
                    </div>
                    <p><strong><i class="bi bi-person"></i> Nom :</strong> {{ user.nom }}</p>
                    <p><strong><i class="bi bi-envelope"></i> Email :</strong> {{ user.email }}</p>
                    <p><strong><i class="bi bi-calendar"></i> Date d'inscription :</strong> {{ user.dateInscription|date('d/m/Y') }}</p>
                </div>
            </div>
        </div>
        <!--lien vers favoris de l'utilisateur-->
        <div class="col-md-8">
            <div class="row">

                <div class="col-md-6 mb-4">
                   <div class="card h-100">
                       <div class="card-header bg-warning text-white">
                           <h5 class="mb-0"><i class="bi bi-heart"></i> Mes favoris</h5>
                       </div>
                   <div class="card-body text-center">
                       <i class="bi bi-heart display-3 text-warning mb-3"></i>
                          <p class="card-text">Consultez vos films préférés.</p>
                              <a href="{{ path('app_profile_favoris') }}" class="btn btn-warning me-2">
                                  <i class="bi bi-heart"></i> Voir mes favoris
                              </a>
                              <a href="{{ path('app_film_index') }}" class="btn btn-outline-warning">
                                <i class="bi bi-plus-circle"></i> Ajouter des favoris
                              </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!--problème d'affichage du footer-->
{% endblock %}